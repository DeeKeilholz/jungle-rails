'use strict';

$(document).ready(() => {

// sets up/adds listener automatically for every time a new card is created which has an #add-item button
// able to add second parameter to the click (#add-item)
$('.products').on('click', '#add-item', () => {
  console.log('image has been clicked')
})


})
